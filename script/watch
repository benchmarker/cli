#!/usr/bin/env stack
-- stack --resolver lts-16.20 script --package process,turtle

{-# LANGUAGE OverloadedStrings #-}

import System.Process
import Turtle

-- Assumes current directory is the project root

main :: IO ()
main = liftIO $ callProcess "stack" ["build", "--file-watch"]
